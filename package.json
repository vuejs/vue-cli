{
  "private": true,
  "workspaces": [
    "packages/@vue/*",
    "packages/test/*"
  ],
  "scripts": {
    "lint": "eslint --fix packages/**/*.js",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.js": ["eslint --fix", "git add"]
  },
  "devDependencies": {
    "debug": "^3.1.0",
    "eslint": "^4.14.0",
    "eslint-plugin-vue-libs": "^2.0.1",
    "husky": "^0.14.3",
    "lerna": "^2.5.1",
    "lint-staged": "^6.0.0"
  }
}
